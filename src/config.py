class Config:
    # 视频假设帧率 (会被视频实际帧率覆盖)
    FPS = 30.0
    
    # 单应性矩阵映射参考点（此处为示例，实际应通过校准工具选取）
    # 像素坐标下的球场角点 (u, v)
    REFERENCE_POINTS_2D = [
        [300, 800], [1620, 800], # 近端底线两角
        [800, 300], [1120, 300]  # 远端底线两角
    ]
    
    # 现实世界对应的球场坐标 (X, Y) 单位: 米
    # 假设场地中心为 (0,0)
    REFERENCE_POINTS_3D = [
        [-5.485, 11.885], [5.485, 11.885],
        [-5.485, -11.885], [5.485, -11.885]
    ]

    # 单视角透视补偿参数
    # 假设摄像机高度 (米) 和 焦距估算 (用于补偿Z轴高度带来的像素偏移)
    CAMERA_HEIGHT = 4.0 
    CAMERA_FOCAL_LENGTH_PX = 1500.0
    
    # 击球高度预估 (米)
    ASSUMED_HIT_HEIGHT = 1.0    # 普通击球 (腰部)
    ASSUMED_SERVE_HEIGHT = 2.8  # 发球击球 (头顶上方)
    
    # 物理极限约束
    MAX_BALL_SPEED_KMH = 250.0       # 网球最大时速过滤
    MIN_HIT_CONFIRM_SPEED_KMH = 40.0 # 确认是击球而非拍球的最低时速
    
    # 场地合法边界 (米) - 用于过滤无效 Bounce
    # 标准场长 23.77m (±11.885), 宽 10.97m (±5.485)
    COURT_X_LIMIT = 6.5   # 左右边线外扩 1米
    COURT_Y_LIMIT = 15.0  # 底线外扩 3米
