# 网球视频分析与数据提取完整方案 (Tennis Video Analysis System)

## 项目概述
本方案旨在基于现有的 `wasb-sbdt-inference` 模型，对双视角摄像头（cam66 和 cam68）的网球比赛视频进行深度分析。目标是提取网球像素轨迹、识别击球与触地事件、区分正反手、计算球员跑动距离以及计算击球瞬时球速。

考虑到现有项目中已经具备了部分能力（如 `wasb` 的球体追踪、部分单应性矩阵脚本和 `yolo` 模型），本方案将以整合与功能扩充为主。

---

## 模块拆解与技术路线

### 1. 网球像素轨迹提取 (Tennis Ball Pixel Trajectory)
**目标:** 提取球在图像中的连续2D像素坐标。
*   **方法:** 直接复用 `wasb-sbdt-inference` 文件夹中的 `wasb_inference.py`。
*   **优化方案:** 
    *   **平滑与插值:** WASB模型在某些帧可能会漏检。需要引入基于卡尔曼滤波 (Kalman Filter) 或三次样条插值 (Cubic Spline Interpolation) 的后处理模块，对断裂的轨迹进行填补和异常值剔除。
    *   **输出:** 得到平滑后的 $(u_t, v_t)$ 像素坐标序列。

### 2. 计算投影矩阵与空间转换 (Homography & Spatial Mapping)
**目标:** 将像素坐标 $(u, v)$ 转换为现实世界场地坐标 $(X, Y)$。
*   **方法:** 单应性矩阵 (Homography Matrix)。
*   **实现步骤:**
    1.  提取球场关键点（如底线、发球线、边线的交点，共至少4个，推荐8-12个以减少误差）。
    2.  定义这些点在现实世界中的标准网球场坐标（例如以球场中心为原点，长 23.77m，宽 10.97m）。
    3.  利用 `cv2.findHomography` 计算投影矩阵 $H$。
    4.  *注：* 根据你工作区现有的 `tennis-3d-tracking` 目录，可以直接复用其中已经计算好的 `homography_matrices.json`，只需针对这两个新视频确认是否适用即可。

### 3. 使用 YOLO 识别人物与姿态 (Player Detection & Pose Estimation)
**目标:** 稳定追踪画面中的两名球员，并获取骨骼关键点用于后续分析。
*   **方法:** 
    *   **目标检测:** 使用 YOLOv11（或 YOLOv8）检测 `person` 类别。
    *   **多目标追踪:** 结合 ByteTrack 算法分配追踪ID，区分近端（Near-court）和远端（Far-court）球员。近端球员的 bounding box 通常 Y 坐标更靠近图像底部。
    *   **姿态估计:** 由于需要识别正反手，普通的 Bounding Box 不够。建议升级到 **YOLOv11-Pose** 或集成 **MediaPipe Pose**，提取球员的手腕、手肘和肩膀关键点。

### 4. 识别击球与触地 (Hit & Bounce Recognition)
**目标:** 从连续轨迹中精准定位击球帧和触地帧。
*   **物理特性分析:** 
    *   网球在空中的运动轨迹（不考虑空气阻力下）近似于抛物线（二次曲线）。
    *   **触地 (Bounce):** 轨迹呈现出 V 字型反弹。在现实垂直轴（或2D像素纵轴）上表现为局部极值（极大值或极小值，取决于摄像机角度），且伴随加速度方向的突变。
    *   **击球 (Hit):** 轨迹的水平方向（X/Y平面）发生急剧逆转。同时，该突变点必定在某位球员的 Bounding Box 附近。
*   **算法设计:**
    1.  **分段多项式拟合 (Piecewise Polynomial Fitting):** 使用 RANSAC 算法对球的轨迹进行抛物线分段拟合。
    2.  **交点提取:** 两段抛物线的交点即为状态改变点（Event Point）。
    3.  **分类:** 
        *   结合球员位置：如果突变点距离某球员中心极近，判定为 **击球 (Hit)**。
        *   结合轨迹高度/方向：如果突变点远离所有球员，且呈现下落后反弹的夹角，判定为 **触地 (Bounce)**。

### 5. 识别击球正反手 (Forehand vs Backhand)
**目标:** 在“击球”事件发生的瞬间，判断是正手还是反手。
*   **前置依赖:** 击球事件发生的帧号、球员的骨骼关键点（特别是手腕和肩膀）。
*   **算法设计:**
    1.  提取击球帧及前后各3帧（形成短序列）。
    2.  判断持拍手：一般为靠近球一侧的手臂，或基于运动幅度最大的手臂判断。假设已知球员为右手持拍。
    3.  **几何位置法:** 观察右手手腕相对于躯干中心（双肩连线中点）的位置。
        *   击球瞬间，右手手腕位于身体右侧，且向左前方挥动 -> **正手 (Forehand)**。
        *   击球瞬间，右手手腕越过身体位于左侧，向右前方挥发 -> **反手 (Backhand)**。
    4.  *进阶方案:* 训练一个轻量级的时空图卷积网络（ST-GCN）或简单的多层感知机（MLP），输入为这7帧内的上半身关键点序列，进行二分类。

### 6. 识别近场人物移动距离 (Near-court Player Movement Distance)
**目标:** 计算特定球员在现实世界中的跑动距离。
*   **算法设计:**
    1.  通过 YOLO+ByteTrack 锁定近场球员（ID）。
    2.  提取每帧该球员 Bounding Box 的**底边中点**（代表双脚着地位置）。
    3.  利用计算好的 **单应性投影矩阵 (Homography)**，将脚部的像素坐标 $(u, v)$ 映射到真实的 2D 球场俯视图坐标 $(X, Y)$（单位：米）。
    4.  应用滑动窗口平均（Sliding Window Average）对真实坐标进行平滑，以消除 Bounding Box 抖动带来的“伪移动”。
    5.  累加相邻帧之间的欧式距离：$Distance = \sum \sqrt{(X_t - X_{t-1})^2 + (Y_t - Y_{t-1})^2}$。

### 7. 实际瞬时球速计算 (Instantaneous Ball Speed Calculation)
**目标:** 计算击球瞬间，网球离开球拍的真实初速度。
*   **难点:** 单应性矩阵通常假设物体在一个2D平面（地面）上，但网球在空中飞行，具有高度（Z轴），直接用2D矩阵计算会产生透视误差。
*   **解决方案 (基于双视角相机的 3D 三角测量):**
    1.  既然有 `cam66` 和 `cam68` 两个视角的同步视频，可以通过**立体视觉三角测量 (Stereo Triangulation)** 计算球的 3D 坐标 $(X, Y, Z)$。
    2.  *如果你现有项目 `tennis-3d-tracking` 中已具备双目参数，强烈建议使用此方法。*
*   **如果降级为单相机估算:**
    1.  假设网球击球点高度 $Z_{hit}$（一般在 0.8m - 1.2m 之间）。
    2.  对像素轨迹进行透视补偿后计算XY平面速度。
*   **瞬时球速计算:**
    1.  获取击球点之后的 3 到 5 帧的真实空间坐标（3D）。
    2.  使用 Savitzky-Golay 滤波器对坐标求导（或直接求差分）：$v_t = \frac{\sqrt{dX^2 + dY^2 + dZ^2}}{dt}$，其中 $dt = \frac{1}{FPS}$。
    3.  取击球后第一段时间的平均速度或最高速度作为 **击球初速度**。

---

## 工程结构建议

为了保持项目整洁，建议在这个新建的 `tennis-video-analysis` 文件夹中采用以下目录结构：

```text
tennis-video-analysis/
│
├── SOLUTION_PLAN.md          # 本方案文档
├── requirements.txt          # 依赖清单 (YOLO, OpenCV, filterpy等)
│
├── data/
│   ├── input/                # 存放视频
│   └── output/               # 存放 CSV 数据和可视化结果
│
├── src/
│   ├── core/
│   │   ├── ball_tracker.py   # 封装 WASB 推理及轨迹平滑
│   │   ├── pose_tracker.py   # 封装 YOLO-Pose 球员与姿态追踪
│   │   └── camera_utils.py   # 单应性矩阵及 3D 映射工具
│   ├── event_detector.py     # 核心：识别击球、触地、正反手
│   ├── metrics_calc.py       # 核心：计算跑动距离、球速
│   └── visualizer.py         # 视频叠加绘制工具 (画轨迹、数据板)
│
└── main_pipeline.py          # 串联所有模块的主执行脚本
```

## 下一步行动建议
1.  **确认相机校准:** 确认 `tennis-3d-tracking` 里的 `cam66/68` 的 calibration 数据是否可直接用于本次的 `2min.mp4` 视频。如果可以，将极大简化球速和距离的计算。
2.  **轨迹平滑测试:** 可以先运行 WASB 生成 CSV，编写一个简单的 Python 脚本对其进行卡尔曼滤波，并寻找转折点来验证“击球/触地”算法。
3.  **升级 YOLO:** 决定是使用 YOLOv11 检测框+启发式规则判断正反手，还是引入 YOLO-Pose。建议直接使用 YOLO-Pose。

如果你希望我开始编写其中的某个具体模块（例如：**球体轨迹平滑与事件检测算法代码**，或 **YOLO 姿态识别与跑动距离计算代码**），请告诉我！